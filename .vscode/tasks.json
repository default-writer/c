{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gcc-build",
            "type": "shell",
            "command": "gcc -g ${fileDirname}/${fileBasename} -o ${fileDirname}/${fileBasenameNoExtension}",
            "problemMatcher": [],
            "group": "test",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}